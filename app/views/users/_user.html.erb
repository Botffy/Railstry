<li>
	<%= link_to ( gravatar_for(user, :size=>30) )+user.name, user %>

	<% if current_user.admin? %>
		| <%=link_to "delete", user, :method=>:delete, :confirm=>"Do you really want to delete #{user.name}?", :title=>"Delete #{user.name}" %>
	<% end %>
</li>
