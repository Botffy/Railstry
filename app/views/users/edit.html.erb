<h1>Edit data</h1>


<%= form_for(@user) do |f| %>

<%= render 'shared/error_messages', :object=>f.object %>

<%= render 'form_fields', :f=>f %>

<div class='actions'>
	<%= f.submit "Sign up!" %>
</div>


<%end%>
