<h1>Edit data</h1>


<%= form_for(@user) do |f| %>

<%= render 'shared/error_messages', :object=>f.object, :msg=>"edit the data" %>

<%= render 'form_fields', :f=>f %>

<div class='actions'>
	<%= f.submit "Update!" %>
</div>

<div>
	<%= gravatar_for @user %>
	<a href="http://gravatar.com/emails">change</a>
<%end%>
