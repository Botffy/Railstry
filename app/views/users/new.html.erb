<h1>Sign up!</h1>


<%= form_for(@user) do |f| %>

<%= render 'shared/error_messages', :object=>f.object , :msg=>"register on the site" %>

<%= render 'form_fields', :f=>f %>

<div class='actions'>
	<%= f.submit "Sign up!" %>
</div>


<%end%>
