<%= form_for @micropost do |f| %>
	<%= render 'shared/error_messages', :object=>f.object, :msg=>"make a post" %>
	<div class='field'>
		<%= f.text_area :content %>
	</div>
	<div class='actions'>
		<%= f.submit "Post!" %>
	</div>
<% end %>
